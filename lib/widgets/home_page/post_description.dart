import 'package:flutter/material.dart';

class PostDescription extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      //TODO: give height and width constraints
      child: Text('this is description'),
    );
  }
}
